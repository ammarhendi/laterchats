[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.12.0", "pnpm i --frozen-lockfile"]

[phases.build]
cmds = ["pnpm build:server"]

[start]
cmd = "pnpm db:push && pnpm start"
